VITE_APP_NAME=Dynamic API Studio

# 平台元数据库（PostgreSQL）——平台自身存储 project/api/datasource/variable/function/log 等元数据。
# 复制本文件为 .env 并填入真实连接信息（.env 已被 git 忽略，不会提交）。
PLATFORM_DB_HOST=localhost
PLATFORM_DB_PORT=5432
PLATFORM_DB_NAME=dynamic_api
PLATFORM_DB_USER=postgres
PLATFORM_DB_PASSWORD=
PLATFORM_DB_POOL_MIN=0
PLATFORM_DB_POOL_MAX=10